name: Build and Push RuoQi Sys Admin

on:
  push:
    branches: [main]
    paths:
      - 'apps/ruoqi-sys-admin/**'
      - 'scripts/ruoqi-sys-admin-build/**'
  workflow_dispatch: # 允许手动触发

env:
  DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
  DOCKER_HUB_ACCESS_TOKEN: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

jobs:
  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install and setup pnpm
        run: |
          echo "=== 安装和设置 pnpm ==="
          # 安装 corepack 并启用 pnpm
          npm install -g corepack@latest
          corepack enable

          # 准备并激活 pnpm
          corepack prepare pnpm@latest --activate

          # 验证 pnpm 安装
          echo "pnpm 版本:"
          pnpm --version

      - name: Run build script
        run: |
          echo "=== 运行构建脚本 ==="
          cd scripts/ruoqi-sys-admin-build

          # 确保脚本有执行权限
          chmod +x build.sh || true

          # 检查脚本是否存在
          if [ ! -f "build.sh" ]; then
            echo "错误: build.sh 脚本不存在"
            exit 1
          fi

          # 运行构建脚本（只构建不运行容器）
          ./build.sh --no-run  # 只构建，不运行容器

      - name: Generate version tag
        id: version
        run: |
          echo "=== 生成版本标签 ==="

          # 获取版本号
          VERSION="1.0.0"
          if [ -f "./apps/ruoqi-sys-admin/package.json" ]; then
            VERSION=$(node -p "require('./apps/ruoqi-sys-admin/package.json').version || '1.0.0'" 2>/dev/null || echo "1.0.0")
          fi

          SHORT_SHA=${GITHUB_SHA::8}
          DATE=$(date +'%Y%m%d')
          TAG="v${VERSION}-${DATE}-${SHORT_SHA}"

          echo "Version: ${VERSION}"
          echo "Commit SHA: ${SHORT_SHA}"
          echo "Date: ${DATE}"
          echo "Full tag: ${TAG}"

          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Push to Docker Hub
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        run: |
          echo "=== 推送镜像到 Docker Hub ==="
          cd scripts/ruoqi-sys-admin-build

          # 检查推送脚本是否存在
          if [ ! -f "push-multi.sh" ]; then
            echo "错误: push-multi.sh 脚本不存在，尝试使用 push.sh"
            if [ ! -f "push.sh" ]; then
              echo "错误: push.sh 脚本也不存在"
              exit 1
            fi
            PUSH_SCRIPT="push.sh"
          else
            PUSH_SCRIPT="push-multi.sh"
          fi

          chmod +x "$PUSH_SCRIPT"

          # 设置环境变量
          export DOCKER_HUB_USERNAME="${{ env.DOCKER_HUB_USERNAME }}"
          export DOCKER_HUB_ACCESS_TOKEN="${{ env.DOCKER_HUB_ACCESS_TOKEN }}"

          echo "使用推送脚本: $PUSH_SCRIPT"

          # 运行推送脚本
          if [ "$PUSH_SCRIPT" = "push-multi.sh" ]; then
            # 使用 push-multi.sh 推送多个标签
            ./push-multi.sh
          else
            # 使用 push.sh 推送单个标签
            ./push.sh "${{ steps.version.outputs.tag }}"
          fi

      - name: Create build summary
        if: success()
        run: |
          echo "## 🎉 RuoQi Sys Admin 构建成功" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ github.event_name }}" != "pull_request" && "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "### 📦 Docker 镜像" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- \`${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin:${{ steps.version.outputs.tag }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin:latest\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin:v${{ steps.version.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 🔗 相关链接" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- [Docker Hub 仓库](https://hub.docker.com/r/${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin)" >> $GITHUB_STEP_SUMMARY
            echo "- [查看所有标签](https://hub.docker.com/r/${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin/tags)" >> $GITHUB_STEP_SUMMARY
          else
            echo "### 🧪 测试构建" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "本次为 PR 或测试构建，镜像未推送到 Docker Hub" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 构建信息" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **版本:** ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **提交:** [${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})" >> $GITHUB_STEP_SUMMARY
          echo "- **触发者:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **工作流:** [${{ github.workflow }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            scripts/ruoqi-sys-admin-build/build.log
          retention-days: 7

  notify:
    name: Notification
    runs-on: ubuntu-latest
    needs: [build]
    if: always()

    steps:
      - name: Send notification
        run: |
          echo "=== 构建通知 ==="
          echo ""

          if [ "${{ needs.build.result }}" = "success" ]; then
            echo "✅ RuoQi Sys Admin 构建成功！"
            echo ""

            if [[ "${{ github.event_name }}" != "pull_request" && "${{ github.ref }}" == "refs/heads/main" ]]; then
              echo "📦 镜像已推送到 Docker Hub:"
              echo "  - ${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin:latest"
              echo "  - ${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin:${{ needs.build.outputs.tag }}"
              echo "  - ${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin:v${{ needs.build.outputs.version }}"
              echo ""
              echo "🔗 查看镜像:"
              echo "  https://hub.docker.com/r/${{ env.DOCKER_HUB_USERNAME }}/ruoqi-sys-admin/tags"
            else
              echo "🧪 测试构建完成"
            fi
          else
            echo "❌ 构建失败！"
            echo ""
            echo "请检查构建日志或下载日志文件查看详情。"
          fi
        env:
          DOCKER_HUB_USERNAME: ${{ secrets.DOCKER_HUB_USERNAME }}
